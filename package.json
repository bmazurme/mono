{
  "name": "mono",
  "version": "1.0.0",
  "private": true,
  "description": "Demo: mono",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=packages/js & npm run dev --workspace=packages/ts",
    "dev:js": "npm run dev --workspace=packages/js",
    "dev:ts": "npm run dev --workspace=packages/ts"
  },
  "engines": {
    "node": ">=24.0.0"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}